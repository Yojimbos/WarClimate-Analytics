apiVersion: apps/v1
kind: Deployment
metadata:
  name: war-climate-analytics-api
  namespace: app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: war-climate-analytics-api
  template:
    metadata:
      labels:
        app: war-climate-analytics-api
    spec:
      serviceAccountName: war-climate-analytics-api
      securityContext:
        fsGroup: 10001
      containers:
        - name: api
          image: acrplaceholder.azurecr.io/war-climate-analytics-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: war-climate-analytics-config
            - secretRef:
                name: war-climate-analytics-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 15
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]

