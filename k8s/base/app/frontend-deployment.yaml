apiVersion: apps/v1
kind: Deployment
metadata:
  name: war-climate-analytics-frontend
  namespace: app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: war-climate-analytics-frontend
  template:
    metadata:
      labels:
        app: war-climate-analytics-frontend
    spec:
      containers:
        - name: frontend
          image: acrplaceholder.azurecr.io/war-climate-analytics-frontend:latest
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
